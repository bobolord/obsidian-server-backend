package routes

import (
	"fmt"
	"github.com/julienschmidt/httprouter"
	"net/http"
)

// func Index(w http.ResponseWriter, r *http.Request, _ httprouter.Params) {
// 	fmt.Fprint(w, "Hello World!")
// }

func Routes(route *httprouter)
indexRoute = route.Group("/"){
    user.GET("/")
}
